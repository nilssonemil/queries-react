import QuestionForm from "../questionform";
import QuestionList from "../questionlist";

const Root = () => {
  return (
    <div>
      <QuestionForm />
      <QuestionList />
    </div>
  );
};

export default Root;
